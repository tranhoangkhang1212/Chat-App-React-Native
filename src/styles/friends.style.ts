import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  text: {
    fontWeight: '500',
  },
  view: {
    marginVertical: 20,
  },
  image: {
    width: 50,
    height: 50,
    borderRadius: 50 / 2,
  },
});
